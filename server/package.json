{
  "name": "gonglui-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx prisma generate",
    "dev": "nodemon src/server.js",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate deploy",
    "seed:admin": "node src/scripts/seedAdmin.js",
    "routes:backfill-owner": "node src/scripts/backfillRouteOwners.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.1",
    "prisma": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
